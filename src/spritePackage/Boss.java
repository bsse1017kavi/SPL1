package spritePackage;

public class Boss extends Enemy
{
    public Boss(double health, double baseDamage, double attackRadius, double posX, double posY, double height, double width, double detectionRadius)
    {
        super(health, baseDamage, attackRadius, posX, posY, height, width, detectionRadius);
    }
}
